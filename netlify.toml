[build]
  base = "new-design"
  command = "npm run build"
  publish = "dist"

# SPA routing (React Router): serve index.html for all paths
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

